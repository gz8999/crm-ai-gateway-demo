# Dataverse Script Safety Inventory

All scripts require an explicit `DATAVERSE_URL`. Write-capable scripts additionally require `DATAVERSE_ENVIRONMENT_KIND=test`, a configured `DATAVERSE_PRODUCTION_HOSTNAMES` denylist, `--confirm-test-environment`, and `--confirm`. Existing phase-specific authorization gates remain in force. Publish/deploy-capable scripts also require `--confirm-publish-or-deploy`.

| Classification | Scripts | Safety behavior |
|---|---|---|
| read-only | `audit-phase1b-l2-final-readonly.mjs`, `audit-phase1b-m0.mjs`, `audit-phase1b-m2b-business-rule-fields.mjs`, `audit-phase1b-m2b-duplicate-labels.mjs`, `audit-phase1b-m2b-form-required.mjs`, `audit-phase1b-m2b-temporary-display-name.mjs`, `audit-phase1c3b-solution-membership.mjs`, `generate-phase1b-dry-run.mjs`, `generate-phase1b-m3-draft-baseline.mjs`, `generate-phase1c0-actual-management-dry-run.mjs`, `generate-phase1c1r-missing-fields-resume-dry-run.mjs`, `generate-phase1c4-subgrid-dry-run.mjs`, `generate-phase1c5r-actual-totals-architecture.mjs`, `generate-phase1c5r0-architecture-correction.mjs`, `generate-phase1c5r0-f1-form-rebind-dry-run.mjs`, `phase1c5r2g-d6-partial-readback.mjs`, `phase1c5r2g-d6-reference-recovery-readback.mjs`, `register-actual-totals-plugin.mjs`, `validate-phase1b-m2b-pilot-business-rule.mjs`, `validate-phase1b-m3c-draft-bpf.mjs`, `validate-phase1c1r-missing-fields-result.mjs` | URL validation only; no metadata or data mutation is permitted. The D6 recovery tools are exact-ID/reference-master GET-only verifiers and expose no write method. The registration tool is local-file-only, defaults to dry-run, and creates zero network requests. Read-only OData actions may use POST without making a write. |
| write-capable | `add-phase1a-fields-to-solution.mjs`, `apply-phase1b-m1-status-reasons.mjs`, `apply-phase1c1-actual-management-table.mjs`, `apply-phase1c2-opportunity-relationship.mjs`, `apply-phase1c3-actual-management-view.mjs`, `apply-phase1c3a-add-view-to-solution.mjs`, `apply-phase1c3c-retry-add-view-to-solution.mjs`, `apply-phase1c5r2d1-registration.mjs`, `create-phase1b-full-form.mjs`, `create-phase1b-full-view.mjs`, `import-location-master-data.mjs`, `patch-phase1b-m2a-demo-fields.mjs`, `phase1c5-synthetic-actuals.mjs`, `phase1c5r2d3b-group1-validation.mjs`, `phase1c5r2d4-group2-child-total.mjs`, `phase1c5r2d5-group3-parent-total.mjs`, `phase1c5r2f-r3c-runtime-probe.mjs`, `phase1c5r2g-d6-full-import.mjs`, `repair-phase1b-form-base-chinese-labels.mjs`, `repair-phase1b-form-visual-labels.mjs` | Refuses production hostnames and missing explicit test/write confirmation. No default URL or implicit write mode. Test-environment use only. The D6 executor is restricted to the frozen Formal-minus-Pilot Token complement, exact read-before-write, official Win/Lose actions, and first-failure safe stop; it has no PATCH, DELETE, Publish, manual BPF, security graph, or Gateway write path. The Location importer validates the external CSV before creating a client, defaults to dry-run, creates only missing active names, and never modifies existing records. The registration executor is resume-only and never creates, updates, or deletes an existing Assembly. Group 1 only enables PreValidation, Group 2 only enables PreOperation, and Group 3 only enables PostOperation parent recalculation; Group 3 restores the Group 1/2-only state on failure. The R3C executor permits one bounded synthetic Account/Opportunity/Coverage/Signal probe, two expected alternate-key rejections, and exact manifest-driven cleanup; it contains no Choice, Publish, Solution, Schema, Form, View, App, BPF, Plugin, Team, Business Unit, or role write path. |
| publish/deploy-capable | `phase1a-full.mjs`, `phase1c5r2e2d4b-polpod-lookup.mjs`, `phase1c5r2e2d4a-timeline-restore.mjs`, `phase1c5r2e2f2-location-schema.mjs`, `phase1c-schema-mvp-core.mjs`, `phase1c5r2f-form-view-security.mjs`, `phase1c5r2f-choice-solution-repair.mjs`, `phase1c5r2f-r3a-bpf-backing-table-repair.mjs`, `phase1c5r2g-d5-dedicated-opportunity-view.mjs` | All write gates plus independent publish/deploy confirmation. The Core Schema executor creates only the approved Opportunity fields, Coverage/Signal entities, relationships, alternate keys and solution membership, then performs one targeted entity publish. The Form/View/Security executor is limited to the approved forms, views, Full Replica form, role privileges, and targeted entity publishing; it never touches the Protected Form, App, BPF, Plugin, or business data. The Choice/Solution repair executor is limited to the approved unmanaged export, 12 existing local picklists, targeted Coverage/Signal publish, and one synthetic probe with exact cleanup; it never deletes Choice options or touches unrelated components. The R3A executor is limited to one shell-only backing-entity solution component add when missing and one unmanaged solution export/package inspection; it does not publish, alter the BPF definition, or write business data. The D5 dedicated-view executor creates or reuses one approved Opportunity SavedQuery, adds only its Solution/App relationships, and publishes only the target Modern App; it cannot write Opportunity or other business records. Broad or production publication remains prohibited. Test-environment use only. |

Credentials, tenant identifiers, production hostnames, and authorization phrases must never be committed. Local environment values belong only in ignored configuration.
